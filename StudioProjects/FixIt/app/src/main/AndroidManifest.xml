<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.fixit.fixit">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".login"
            android:label="@string/title_activity_login"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        <activity
            android:name=".UserProfile"
            android:label="@string/title_activity_user_profile"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".device"
            android:label="@string/title_activity_device"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".brand_c"
            android:label="@string/title_activity_brand"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".brand_p"
            android:label="@string/title_activity_brand_p"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".brand_pp"
            android:label="@string/title_activity_brand_pp"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".model_apple"
            android:label="@string/title_activity_model_apple"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>